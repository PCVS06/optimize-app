#!/bin/sh
set -eu
BIN_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
RESOURCES_DIR=$(CDPATH= cd -- "${BIN_DIR}/.." && pwd)
HELPER="${RESOURCES_DIR}/../Frameworks/Optimize Helper.app/Contents/MacOS/Optimize Helper"
exec env ELECTRON_RUN_AS_NODE=1 PATH="${BIN_DIR}:${PATH:-/usr/bin:/bin}" "$HELPER" "${RESOURCES_DIR}/pi-runtime/node_modules/npm/bin/npm-cli.js" "$@"
