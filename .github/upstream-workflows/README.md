# Inactive upstream workflows

These recipes are retained from Paseo for reference. They are outside GitHub’s workflow directory and do not run. Optimize uses the workflows in `.github/workflows/`; review and replace all ownership and infrastructure settings before reusing any archived recipe.
